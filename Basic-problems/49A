So, Here in this problem we just have to find if the second string is present in the first string .

#include <bits/stdc++.h>
using namespace std;
int main() {
	string s,s1,s2="AaEeIiOoUuYy";
	while(cin>>s)s1+=s; //cout<<s1;
	int a=s2.find(s1[s1.size()-2]); //cout<<a;
	if(a<=s2.size()) cout<<"YES";
	else cout<<"NO";
	return 0;
}
