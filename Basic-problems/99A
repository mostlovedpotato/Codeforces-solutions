Here, In this problem we just have to implement whatever rules are given by the king for the rounding of the number.

#include<bits/stdc++.h>
 
using namespace std;
 
int main(){

string s;

cin>>s;

int d=s.find(".");
if(s[d-1]=='9') 
  cout<<"GOTO Vasilisa.";
else 
  if(s[d+1]>='5')
    { s[d-1]++;
      cout<<s.substr(0,d);
     }
else 
  cout<<s.substr(0,d);
}
