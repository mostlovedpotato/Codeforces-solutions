Here, In this problem we just have to find the max of the three times for each green , blue , red so we take out like,

#include <iostream>
#include <algorithm>
 
using namespace std;
 
int main() {
	int r, g, b;
	cin >> r >> g >> b;
	cout << max(((r + 1) / 2 - 1) * 3, max(1 + ((g + 1) / 2 - 1) * 3, 2 + ((b + 1) / 2 - 1) * 3)) + 30;
	return 0;
}
