So, Here we are just have to go on comparing each digits in the numbers and then if they are same we add ' 1 ' to string
otherwise ' 0 ' .

#include <bits/stdc++.h>
using namespace std;
int main() {
	string A,B;cin>>A>>B;
	int s=A.size();
	for(int i=0;i<s;i++)cout<<(A[i]!=B[i]);
}
