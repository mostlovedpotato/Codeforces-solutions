In this problem as we can see we want to calculate if the camels split or not.
so we check if two adjacent positions like 

a[i]+b[i]==a[j] and a[j] +b[j]==a[i]    within two for loops one looping over other like below

#include<iostream>
#include<algorithm>
#include<vector>
using namespace std;
#define ll long long int
int main()
{
	ll n;cin>>n; ll a[n],b[n];
	for(ll i=0;i<n;i++)cin>>a[i]>>b[i];
	for(ll i=0;i<n-1;i++){
		for(ll j=i+1;j<n;j++){
		if(a[i]+b[i]==a[j] and a[j] +b[j]==a[i]){cout<<"YES"; return 0;}}}
		cout<<"NO";
	
}
