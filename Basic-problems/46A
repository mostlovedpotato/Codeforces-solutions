So, Here we see in this problem we just have to print the corresponding numbers when we go on adding the numbers so we go in the loop lol.

#include<iostream>
#include<math.h>
using namespace std;
int main(){
int n;
int d=1;
int i,j;
cin>>n;
for(i=1;i<n;i++)
      {
        d=d+i;
        if(d>n)
          d=d%n;
        cout<<d<<' ';
      }
}
